 Ext.onReady(       // Ext.onReady - вызов функции по загрузке DOM
      function(){ Ext.MessageBox.alert(   // вызов аlert'а
      'Мой заголовок', // заголовок окна  сообщения
      'Hello, ExtJS World!'   // текст сообщения
       );
});